<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.praktikum_mobile_programming_2_revisi">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <application
        android:name=".UI.CrudRoomApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Praktikummobileprogramming2revisi">
    tools:ignore="AllowBackup,UnusedAttribute">
    <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.provider"
        android:exported="false"
        android:grantUriPermissions="true">
        <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
    </provider>

    <activity android:name=".UI.roomData.ViewRoomDataActivity" />
    <activity android:name=".UI.roomData.AddRoomDataActivity" />
    <activity android:name=".UI.roomData.RoomDataActivity" />
    <activity android:name=".UI.sharedPref.SharedPrefActivity" />
    <activity android:name=".UI.ReciverActivity" />
    <activity android:name=".UI.WithFragmentActivity" />
    <activity android:name=".UI.HomeActivity" />
    <activity
        android:name=".UI.MainActivity"
        android:launchMode="singleInstance">

        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <service android:name=".service.TimerService" android:enabled="true" />
    <activity android:name=".service.TimerServiceActivity"/>
    <activity android:name=".UI.activity.MainActivity2" />
    <activity android:name=".UI.activity.TambahDanUbahDataActivity" />
    <activity android:name=".UI.LoginActivity" />
    <activity android:name=".UI.ProfileActivity" />
</application>

    </manifest>